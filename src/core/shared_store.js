const store = {
    config: {
        debug: false,
        redirectUri: "http://localhost:8080/callback",
        headless: true,
        updateSize: 30,
        updateIntervalHours: 6,
        channels: []
    }
};

module.exports = store;